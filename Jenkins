# Updated Jenkins file

task('Show package info') {
    if (fileExists('package.json')) {
        // Execute npm commands here
        echo 'Running npm commands...'
    } else {
        echo 'package.json does not exist, skipping this stage.'
    }
}